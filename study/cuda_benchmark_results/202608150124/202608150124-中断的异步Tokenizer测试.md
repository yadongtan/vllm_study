# 202608150124 - 中断的异步 Tokenizer 测试

## 状态

本目录是异步 Tokenizer 优化的第一次正式测试尝试。外层命令设置了过短的
5 秒执行时限，测试脚本收到终止信号后按清理逻辑关闭了 benchmark、资源监控和
模型服务。

目录中仅留下启动基线、并发 1 的部分资源采样和服务日志，没有形成完整的
1/4/8 并发结果，因此本目录不得用于性能比较。

完整测试已在 `202608150126` 目录重新执行。
